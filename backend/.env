PORT=5000

# ml-service (fake review + phishing URL) — run from the ml-service/ folder:
#   uvicorn src.main:app --reload --port 8000
FASTAPI_URL=http://localhost:8000

# product_scam_ml service (product risk) — run from product_scam_ml/ml-service/:
#   uvicorn main:app --reload --port 8001
PRODUCT_FASTAPI_URL=http://localhost:8001

